@inherits LayoutComponentBase

@* Deliberately a pass-through — Landing.razor draws its own full-width header/footer, unlike
   MainLayout (authenticated app chrome) or AuthLayout (centered auth-form card). *@
@Body
